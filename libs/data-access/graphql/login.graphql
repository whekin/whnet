mutation LoginOrSignUp($nickname: String!, $password: String!) {
    loginOrSignUp(
        data: {
            nickname: $nickname
            password: $password
        }
    ) {
        token
    }
}